package services;

import requests.JoinRequest;
import responses.JoinResponse;

/**
 * Join Service class, makes a join request, than returns response
 */
public class JoinService {
    /**
     * Join
     * @param req request
     * @return response
     */
    public JoinResponse join(JoinRequest req){
        return null;
    }
}
